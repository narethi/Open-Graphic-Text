This is a library that uses the FreeType library to generate a series of textures
so that they can drawn using any program that uses textures. Currently the library
is not in a static or dynamic library format, and the suggested way to use it is in
the source.cpp file